language: php

php:
  - 7.0
  - 7.1
  - 7.2
  - hhvm
  - nightly

matrix:
  allow_failures:
  - php: hhvm
  - php: nightly

sudo: false

cache:
  directories:
    - $HOME/.composer/cache

install: composer install
script: php vendor/bin/phpunit
